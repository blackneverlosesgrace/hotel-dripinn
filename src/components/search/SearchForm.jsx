export default function SearchForm({
  onSearch
}) {
  return (
    <form onSubmit={onSearch} className="bg-white p-6 rounded-lg shadow space-y-4">
      {/* city + location */}
      {/* dates */}
      {/* guests + duration */}
      {/* search button */}
    </form>
  );
}
