name: Deploy to Vercel

on:
  push:
    branches:
      - main
    paths:
      - 'server/**'
      - '.github/workflows/deploy-vercel.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deployment
        run: |
          curl -X POST https://api.vercel.com/v1/integrations/deploy/prj_xAzpR84G1lEnMCsn0KoMRPcQPynJ/MTItCzOXfX
      
      - name: Deployment triggered successfully
        run: echo "✅ Backend deployment triggered on Vercel"
